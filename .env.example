# ── Emergent ──────────────────────────────────────────────────────────
# Project-scoped token (emt_*). Required for stdio mode.
# In HTTP mode, clients send their own token as the Bearer header.
EMERGENT_TOKEN=emt_your_token_here

# Optional: Emergent server URL (default: http://localhost:3002)
# EMERGENT_URL=http://localhost:3002

# Optional: Required when using standalone API keys instead of project-scoped tokens
# EMERGENT_PROJECT_ID=

# ── Transport ────────────────────────────────────────────────────────
# Transport mode: "stdio" (default) or "http"
SPECMCP_TRANSPORT=http

# HTTP mode settings (only used when SPECMCP_TRANSPORT=http)
SPECMCP_PORT=21452
# SPECMCP_HOST=0.0.0.0
# SPECMCP_CORS_ORIGINS=*

# ── Logging ──────────────────────────────────────────────────────────
# SPECMCP_LOG_LEVEL=info
